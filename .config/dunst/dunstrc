# See dunst(5) for all configuration options
[global]
    ### Display ###
    monitor = 0
    follow = mouse
    width = 300
    height = 300
    origin = top-right
    offset = 10x40
    notification_limit = 0
    progress_bar = true
    progress_bar_height = 16
    progress_bar_frame_width = 1
    progress_bar_min_width = 150
    progress_bar_max_width = 400
    progress_bar_corner_radius = 8
    indicate_hidden = yes
    separator_height = 2
    padding = 15
    horizontal_padding = 15
    text_icon_padding = 15
    frame_width = 2
    frame_color = "#313244"
    gap_size = 10
    separator_color = frame
    sort = yes
    idle_threshold = 120
    font = JetBrainsMono Nerd Font Medium 10
    line_height = 0
    markup = full
    format = "<span weight='bold'>%s</span>\n%b"
    alignment = left
    vertical_alignment = center
    show_age_threshold = 60
    ellipsize = middle
    ignore_newline = no
    stack_duplicates = true
    hide_duplicate_count = false
    show_indicators = false
    # Experimental: Recursive icon lookup. You can set a single theme, instead of having to
    # define all lookup paths.
    enable_recursive_icon_lookup = true
    # Set icon theme (only used for recursive icon lookup)
    icon_theme = a_hyb_WhiteSur
    icon_corner_radius = 12
    icon_position = left
    min_icon_size = 16
    max_icon_size = 48
    icon_path = $HOME/.icons/a_hyb_WhiteSur/status/32:$HOME/.icons/a_hyb_WhiteSur/status/24:$HOME/.icons/a_hyb_WhiteSur/devices/24
    sticky_history = yes
    history_length = 20
    # dmenu path, usage example, "dunstctl context" after mounting drive
    dmenu = /usr/bin/wofi --conf=$HOME/.config/wofi/config-dunst -d dunst:
    # Browser for opening urls in context menu.
    browser = /usr/bin/firefox -new-tab
    always_run_script = true
    title = Dunst
    class = Dunst
    corner_radius = 15
    ignore_dbusclose = false
    layer = overlay
    force_xwayland = false
    mouse_left_click = close_current
    mouse_middle_click = do_action, close_current
    mouse_right_click = close_all

[experimental]
    # Calculate the dpi to use on a per-monitor basis.
    # per_monitor_dpi = false

[urgency_low]
    background = "#1e1e2eD9"
    foreground = "#d9e0ee"
    timeout = 5
    highlight = "#89b4fa"

[urgency_normal]
    background = "#1e1e2eD9"
    foreground = "#d9e0ee"
    timeout = 5
    highlight = "#89b4fa"

[urgency_critical]
    background = "#1e1e2eD9"
    foreground = "#f38ba8"
    frame_color = "#f38ba8"
    highlight = "#f38ba8"
    timeout = 0

[mpd]
    # for script $HOME/.local/bin/songinfo
    stack_tag = "mpd"
    default_icon = "audio-x-generic"
    urgency = "normal"
    background = "#1e1e2eD9"
    foreground = "#d9e0ee"
    frame_color = "#f9e2af"
    timeout = 5
    highlight = "#89b4fa"

# vim: ft=cfg
